# Changelog

## 1.0.25 - 2026-07-14

- 修复 Android 更新下载完成后只停留在下载、不自动拉起系统安装器的问题。
- 让更新任务接收系统 `DownloadManager` 完成广播；仍先校验 APK SHA-256，再通过 FileProvider 交给系统安装器，最后由系统完成用户确认。

## 1.0.24 - 2026-07-14

- 将底部迷你播放器重做为单行控制条：封面、歌曲信息、播放控制保持同一水平线，进度改为底边细线提示。
- 修复 Android 更新下载完成后不自动打开系统安装器的问题，补充更新任务持久化、前台恢复检查和 FileProvider URI 授权。
- 保留更新包 SHA-256 校验，安装前仍由系统安装器完成最终确认。

## 1.0.23 - 2026-07-14

- 重做桌面端与移动端视觉系统：暖纸色背景、靛蓝播放器、珊瑚强调色和统一卡片层级。
- 首页新增聆听概览卡片，空数据、加载中和离线状态也保持清晰的视觉锚点。
- 统一搜索、歌单、歌曲列表、弹窗、底部播放器和沉浸式播放器的圆角、阴影、动效与焦点状态。
- 增加移动端响应式细节和减少动态效果偏好支持，不改变原有播放、歌单、账号和存储逻辑。

## 1.0.22 - 2026-07-13

- 修复 Android 自动更新下载失败：`DownloadManager` 更新请求改用符合 HTTP 规范的 ASCII `User-Agent`，避免部分 Android 版本在入队时直接拒绝请求。
- 保留 GitHub Release 重定向、SHA-256 校验和系统安装确认流程不变。

## 1.0.21 - 2026-07-13

- 让“歌曲淡入淡出”和“歌词来源”设置真正参与播放与歌词请求；网络歌词失败时保留内嵌歌词。
- 在线搜索不可用时保留匹配的本地曲库结果，并明确标示为离线本地结果。
- 恢复备份改为先校验、预览，再由用户明确选择合并或覆盖；拒绝外部应用、畸形、未引用或超限本地文件条目。
- 自动检查更新现在只读取版本与更新日志；下载 APK、安装 APK 和桌面更新均需用户点击。
- 移除任意自定义 API 后端地址，账号、歌单、历史和下载记录只请求本机同源服务；Android Release 仅允许内嵌 loopback 后端的明文连接。
- Bili 账号同步会读取全部收藏夹和每个收藏夹的全部分页内容，不再静默截断前 8 个收藏夹或前 60 条。

## 1.0.20 - 2026-07-12

- 更新检查结果展示当前版本到最新版本之间的完整 Release 更新说明。
- 设置页按版本顺序显示更新日志，用户可在更新前判断是否需要升级。
- 更新历史说明请求失败时仍保留最新版本检查和升级能力。

## 1.0.19 - 2026-07-12

- 新增 APK 与桌面网页的“自动检查更新”开关，启动时和每 6 小时检查固定 GitHub Release。
- APK 下载后校验 GitHub SHA-256，再交给 Android 系统安装器，不卸载应用、不清除数据。
- 桌面本地服务仅在工作区干净且由启动器授权时执行 `git pull --ff-only`，完成后自动重建并重启。
- 新增更新 API、设置持久化和 API/E2E 回归测试。

## 1.0.18 - 2026-07-12

- 修复网易云账号同步歌单只加载前 60 首的问题，改为按完整歌单上限加载并保留完整可播放校验。
- 新增超过 60 首网易云账号歌单的 API 回归测试。

## 1.0.17 - 2026-07-12

- 修复网易云歌单详情固定只加载 20 首歌曲的问题，改为加载接口可提供的完整歌单（最多 1000 首）。
- 当详情接口只返回前 20 首但提供完整 `trackIds` 时，自动调用 `track_all` 补齐歌曲。
- 新增超过 20 首歌单的 API 和桌面 E2E 回归测试。

## 1.0.16 - 2026-07-12

- 开启“自动缓存”后，远程歌曲播放时会写入本地缓存和下载记录，并保持当前音频地址、播放状态和进度不变。
- 开启“启动时播放”后，重载会尝试恢复持久化播放队列；浏览器禁止自动播放时统一显示可读提示，点击播放即可继续。
- 共享状态增加更新时间和串行写入保护，旧页面的延迟保存不会覆盖新页面的歌单、队列或设置。
- 共享状态合并按更新时间保留较新的设置，避免重载时丢失自动播放等选项。
- 新增自动缓存、启动恢复、旧共享写入和自动播放异常的回归测试。

## 1.0.15 - 2026-07-12

- 修复快速切换歌单时，旧歌单的延迟播放解析结果覆盖当前歌单播放列表的问题。
- 统一首页歌单卡片、我的歌单列表和歌单详情的歌曲总数口径，优先显示歌单总曲目数。
- 新增歌单切换竞态和歌单数量一致性回归测试。

## Unreleased

- 新增 Windows 与 macOS 桌面版一键启动入口，自动准备本地 Node.js、安装依赖、构建、启动服务并打开浏览器。
- Node.js 下载使用官方 SHA-256 校验，不要求管理员权限，也不修改系统环境。
- 移除旧 Windows 启动脚本的机器专用路径和强制结束端口进程行为。
- 自动清理 macOS 外置盘生成目录中的 AppleDouble 元数据，避免依赖模块被误加载。

## 1.0.14 - 2026-07-11

- 修复下载当前正在播放的未缓存歌曲时，替换音频地址导致播放中断和进度重置的问题。
- 下载完成后保留当前播放会话的远程音频地址，同时正常写入本地缓存和下载记录。
- 新增回归测试，验证下载期间 `audio.src` 不变、不产生 `emptied` 事件、不暂停且进度持续增长。

## 1.0.13 - 2026-07-11

- 修复搜索临时播放队列长时间暂停、进入后台或 App/WebView 重载后无法恢复播放。
- 恢复播放时刷新过期 FLAC 签名，并保留浏览器用户手势授权。
- 播放、上一首、下一首和队列选歌共用同一恢复逻辑，同时适用于桌面端与 Android。
- 新增持久化临时队列 reload 回归测试。
- 修复 Android Release 构建时 AppleDouble 清理器与 CMake 临时文件的 ENOENT 竞态。
