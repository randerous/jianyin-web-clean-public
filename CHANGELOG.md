# Changelog

## 1.0.32 - 2026-08-11

- 新增 WebAudio 均衡器音效系统：默认「均衡·原声 Hi-Fi」微笑曲线，另有饱满、人声、古典、摇滚 4 个预设与「原声（关闭）」完全旁路。
- 均衡器强度滑块（0–100%）可实时缩放各频段增益；设置与强度跨会话持久化，播放中切换即时生效。
- 采用 10 段 ISO 均衡（31Hz–16kHz，peaking BiquadFilterNode），AudioContext 仅在用户播放手势内接线，启动自动播放等非手势路径保持直通，避免静音回归。
- 修复恢复备份合并时均衡器设置被共享状态默认值覆盖的问题（共享状态本不包含设置字段）。
- 修复 e2e 清理上下文测试中 init script 闭包引用模块变量导致注入失效的问题（注入参数需经 arg 显式传入）。

## 1.0.31 - 2026-07-19

- 修复正式 Windows EXE 无法自动升级的问题：旧版启动器不再依赖不存在的 `.git` 工作区，而是直接读取固定 GitHub Release 的 Windows 资产。
- 更新前校验 Release 版本、GitHub 下载地址和 SHA-256；校验通过后由独立更新脚本在旧进程退出后替换并重启启动器。
- Windows 更新只替换启动器本身，不删除、不覆盖 `%LOCALAPPDATA%\\Jianyin\\state.json` 等用户数据。
- 增加正式 Windows 打包模式下更新可用性和启动器自更新回归测试。

## 1.0.30 - 2026-07-19

- 修复共享状态迁移持久化失败时仍返回成功、导致客户端持续提交必然失败的保存请求的问题。
- 读取共享状态失败时不再误判为可写状态；本地编辑继续保留待同步状态，不再触发注定失败的后台保存或“共享歌单保存失败”提示。
- 增加服务端读取失败和桌面端待同步行为的回归测试，覆盖迁移失败、pagehide 和本地编辑场景。

## 1.0.29 - 2026-07-19

- 修复已下载歌曲在重载、长时间暂停或后台恢复后仍使用过期远程链接的问题；播放会优先读取 IndexedDB 本地缓存，首次点击即可继续。
- 修复下载或自动缓存当前歌曲时替换正在播放的音频地址、打断播放并重新开始的问题；缓存完成后保持当前音源、进度和播放状态不变。
- 修复慢速解析、快速切歌、暂停和歌单切换之间的竞态，过期异步结果不再覆盖或停止当前歌曲。
- 共享歌单升级为带服务端 revision、幂等 writeId 和删除墓碑的 v2 协议；并发页面不会复活已删除歌单，也不会丢失另一页面新增的内容。
- 修复初始共享读取期间编辑歌单可能丢失、临时断网后本次会话不再同步、稀疏远端状态遗漏删除或新增，以及主动重新添加内容仍被旧墓碑过滤的问题。
- 共享状态仅保存稳定的歌单与收藏元数据，不再上传播放队列、历史、设置、临时链接或本地文件标识；旧状态迁移前会创建权限为 0600 的原始备份。
- 旧本地歌曲和歌单改用确定性的匿名共享身份；同一歌曲在歌单、收藏或跨歌单移动后仍能关联本机缓存，删除记录不再包含文件名或歌手文本。
- 修复伪装来源的本地 ID 与本机缓存关联不一致、超长 FLAC 删除墓碑失配，以及共享状态错误响应泄露本机路径的问题。
- 修复大型歌单触发“共享歌单保存失败”的请求大小限制，并让性能测试按新协议校验每次状态重置，避免用例间数据串扰。
- Android 正式构建优先使用 `/Volumes/JianyinGradle` 的 APFS 稀疏镜像，无法挂载时回退本机缓存，不再把 Gradle 工作目录放在 exFAT；Capacitor 同步后会自动补齐 Maven 镜像，并在构建前后强制校验历史发布证书。
- Windows 启动器运行时缓存版本更新至 1.0.29，避免新版 EXE 继续复用旧版 1.0.20 后端文件。

## 1.0.28 - 2026-07-17

- 视觉重设计：全站配色从冷蓝/紫色改为"暖阳纸感"主题，亮色为米白 `#f6f3ed` + 朱柿 `#d4594a`，暗色为暖炭 `#131318` + 柔朱 `#e0705f`。
- 导航、按钮、输入框统一为胶囊形状；卡片、播放条、模态采用更小圆角和更柔和的阴影，减少视觉噪音。
- 首页横幅从蓝紫渐变改为暖柿渐变，迷你播放条、沉浸式播放器底色同步为暖色。
- 按钮悬停位移、阴影减淡，过渡更克制；焦点环颜色与品牌色对齐。
- 保留响应式与无障碍契约：320/360/390/430/768px 宽度回归、键盘焦点环、reduced-motion 兼容不变。

## 1.0.27 - 2026-07-15

- 修复“我的歌单”卡片把整个内容按钮错误限制为 64px，导致歌曲数量逐字换行、标题和删除按钮挤在一起的问题。
- 歌单封面改用 `clamp()` 响应式尺寸，内容区使用 `minmax(0, 1fr)` 自动占满剩余宽度，长标题单行省略。
- 统一移动端页面 gutter、顶部导航、页面标题和歌单间距的响应式变量，修复 320px 窄屏约 3px 的横向溢出。
- 新增 320、360、390、430、768px 多宽度回归测试，覆盖文本宽度、导航边界、标题位置、操作区和页面横向溢出。

## 1.0.26 - 2026-07-15

- 重构手机端导航层级：首页、搜索和我的迁移到顶部吸附导航，不再占用第二条底栏。
- 底部只保留一条迷你播放器，继续提供封面、歌曲信息、播放/暂停和下一首，并可点击进入完整播放器。
- 针对 390px 与 430px 手机宽度优化品牌、页面入口和安全区间距；桌面端继续使用左侧导航和原有播放器布局。
- 新增移动端结构回归测试，验证顶部导航切换、底部单栏布局和提示信息防遮挡。

## 1.0.25 - 2026-07-14

- 修复 Android 更新下载完成后只停留在下载、不自动拉起系统安装器的问题。
- 让更新任务接收系统 `DownloadManager` 完成广播；仍先校验 APK SHA-256，再通过 FileProvider 交给系统安装器，最后由系统完成用户确认。

## 1.0.24 - 2026-07-14

- 将底部迷你播放器重做为单行控制条：封面、歌曲信息、播放控制保持同一水平线，进度改为底边细线提示。
- 修复 Android 更新下载完成后不自动打开系统安装器的问题，补充更新任务持久化、前台恢复检查和 FileProvider URI 授权。
- 保留更新包 SHA-256 校验，安装前仍由系统安装器完成最终确认。

## 1.0.23 - 2026-07-14

- 重做桌面端与移动端视觉系统：暖纸色背景、靛蓝播放器、珊瑚强调色和统一卡片层级。
- 首页新增聆听概览卡片，空数据、加载中和离线状态也保持清晰的视觉锚点。
- 统一搜索、歌单、歌曲列表、弹窗、底部播放器和沉浸式播放器的圆角、阴影、动效与焦点状态。
- 增加移动端响应式细节和减少动态效果偏好支持，不改变原有播放、歌单、账号和存储逻辑。

## 1.0.22 - 2026-07-13

- 修复 Android 自动更新下载失败：`DownloadManager` 更新请求改用符合 HTTP 规范的 ASCII `User-Agent`，避免部分 Android 版本在入队时直接拒绝请求。
- 保留 GitHub Release 重定向、SHA-256 校验和系统安装确认流程不变。

## 1.0.21 - 2026-07-13

- 让“歌曲淡入淡出”和“歌词来源”设置真正参与播放与歌词请求；网络歌词失败时保留内嵌歌词。
- 在线搜索不可用时保留匹配的本地曲库结果，并明确标示为离线本地结果。
- 恢复备份改为先校验、预览，再由用户明确选择合并或覆盖；拒绝外部应用、畸形、未引用或超限本地文件条目。
- 自动检查更新现在只读取版本与更新日志；下载 APK、安装 APK 和桌面更新均需用户点击。
- 移除任意自定义 API 后端地址，账号、歌单、历史和下载记录只请求本机同源服务；Android Release 仅允许内嵌 loopback 后端的明文连接。
- Bili 账号同步会读取全部收藏夹和每个收藏夹的全部分页内容，不再静默截断前 8 个收藏夹或前 60 条。

## 1.0.20 - 2026-07-12

- 更新检查结果展示当前版本到最新版本之间的完整 Release 更新说明。
- 设置页按版本顺序显示更新日志，用户可在更新前判断是否需要升级。
- 更新历史说明请求失败时仍保留最新版本检查和升级能力。

## 1.0.19 - 2026-07-12

- 新增 APK 与桌面网页的“自动检查更新”开关，启动时和每 6 小时检查固定 GitHub Release。
- APK 下载后校验 GitHub SHA-256，再交给 Android 系统安装器，不卸载应用、不清除数据。
- 桌面本地服务仅在工作区干净且由启动器授权时执行 `git pull --ff-only`，完成后自动重建并重启。
- 新增更新 API、设置持久化和 API/E2E 回归测试。

## 1.0.18 - 2026-07-12

- 修复网易云账号同步歌单只加载前 60 首的问题，改为按完整歌单上限加载并保留完整可播放校验。
- 新增超过 60 首网易云账号歌单的 API 回归测试。

## 1.0.17 - 2026-07-12

- 修复网易云歌单详情固定只加载 20 首歌曲的问题，改为加载接口可提供的完整歌单（最多 1000 首）。
- 当详情接口只返回前 20 首但提供完整 `trackIds` 时，自动调用 `track_all` 补齐歌曲。
- 新增超过 20 首歌单的 API 和桌面 E2E 回归测试。

## 1.0.16 - 2026-07-12

- 开启“自动缓存”后，远程歌曲播放时会写入本地缓存和下载记录，并保持当前音频地址、播放状态和进度不变。
- 开启“启动时播放”后，重载会尝试恢复持久化播放队列；浏览器禁止自动播放时统一显示可读提示，点击播放即可继续。
- 共享状态增加更新时间和串行写入保护，旧页面的延迟保存不会覆盖新页面的歌单、队列或设置。
- 共享状态合并按更新时间保留较新的设置，避免重载时丢失自动播放等选项。
- 新增自动缓存、启动恢复、旧共享写入和自动播放异常的回归测试。

## 1.0.15 - 2026-07-12

- 修复快速切换歌单时，旧歌单的延迟播放解析结果覆盖当前歌单播放列表的问题。
- 统一首页歌单卡片、我的歌单列表和歌单详情的歌曲总数口径，优先显示歌单总曲目数。
- 新增歌单切换竞态和歌单数量一致性回归测试。

## Unreleased

- 新增 Windows 与 macOS 桌面版一键启动入口，自动准备本地 Node.js、安装依赖、构建、启动服务并打开浏览器。
- Node.js 下载使用官方 SHA-256 校验，不要求管理员权限，也不修改系统环境。
- 移除旧 Windows 启动脚本的机器专用路径和强制结束端口进程行为。
- 自动清理 macOS 外置盘生成目录中的 AppleDouble 元数据，避免依赖模块被误加载。

## 1.0.14 - 2026-07-11

- 修复下载当前正在播放的未缓存歌曲时，替换音频地址导致播放中断和进度重置的问题。
- 下载完成后保留当前播放会话的远程音频地址，同时正常写入本地缓存和下载记录。
- 新增回归测试，验证下载期间 `audio.src` 不变、不产生 `emptied` 事件、不暂停且进度持续增长。

## 1.0.13 - 2026-07-11

- 修复搜索临时播放队列长时间暂停、进入后台或 App/WebView 重载后无法恢复播放。
- 恢复播放时刷新过期 FLAC 签名，并保留浏览器用户手势授权。
- 播放、上一首、下一首和队列选歌共用同一恢复逻辑，同时适用于桌面端与 Android。
- 新增持久化临时队列 reload 回归测试。
- 修复 Android Release 构建时 AppleDouble 清理器与 CMake 临时文件的 ENOENT 竞态。
